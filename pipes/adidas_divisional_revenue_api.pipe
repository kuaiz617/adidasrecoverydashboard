TOKEN dashboard_read READ
DESCRIPTION >
    adidas region x product revenue (EUR millions).

NODE result
SQL >
    SELECT
        quarter,
        region,
        product,
        revenue_m,
        prior_year_revenue_m,
        reported_change_pct,
        currency_neutral_change_pct
    FROM adidas_divisional_revenue
    ORDER BY quarter

TYPE ENDPOINT
